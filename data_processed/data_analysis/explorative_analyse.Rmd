---
title: "Explorative Analyse"
author: "Alexander Marquard / 11779957"
date: "2/12/2022"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
library(feasts)
library(gridExtra)
library(mgcv)
library(forecast)
library(interactions)
```

```{r prep}
final_df <- readRDS("../final_df.RDS")
final_comp <- final_df
final_df <- final_df %>% filter(Meldedatum <= "2022-01-30")
impfungen <- readRDS("../../data_truth/RKI/impfungen.Rds")

N <- sum(unique(final_df$population))
```

Wie die Hospitalisierung funktioniert:
Die hospitalisierten Covid-19 Fälle werden zeitlich nach Meldedatum ausgewiesen.
Je nach Meldehistorie kann das Meldedatum vor, zeitgleich oder nach dem
Hospitalisierungsdatum liegen. 
Vor ist z.B. der Fall, wenn eine Person wegen einem anderen Grund hospitalisiert 
wird und Corona erst im Krankenhaus festgestellt wird. In diesem Fall wird 
der Fall an das zuständige Gesundheitsamt übermittelt und das Meldedatum ist somit 
später, als das Hispitalisierungsdatum.
[https://github.com/robert-koch-institut/COVID-19-Hospitalisierungen_in_Deutschland#zeitlicher-bezug-der-hospitalisierten-covid-19-f%C3%A4lle]
Häufig jedoch liegt das Meldedatum und das Hospitalisierungsdatum eng beieinander.
Erfolgt die Hospitalisierung mehr als 7 Tage nach der Meldung, dann werden diese 
Fälle nachgemeldet.
(grafisch in PP)

Die Hospitalisierungsrate kann auf verschiedene Wege gemessen werden.
Zum einen gibt es die weit verbreitete und mediale genutzte Möglichkeit, 
hospitalisierte Fälle pro Wochepro 100000 Einwohner zu berichten

```{r}
p_rate_100 <- final_df %>%
  group_by(Kalenderjahr, Kalenderwoche) %>%
  summarise(Hospitalisierung = sum(Hospitalisierung), 
            Neuerkrankung = sum(Neuerkrankung)) %>%
  mutate(hosprate = Hospitalisierung/N * 100000)

png(filename="./Bilder/Hospitalisierungsrate.png", width = 2000, height = 300)

ggplot(p_rate_100) +
  geom_line(aes(x = as.Date(paste("1",Kalenderwoche, Kalenderjahr, sep = "-"), 
                            format = "%u-%W-%Y"), 
                y = hosprate), size = 1.25) + 
  theme_minimal() + xlab("Reported date") + ylab("Hospitalization rate") +
  theme(text = element_text(size = 25))

dev.off()
```
Zum anderen kann die Hospitalisierung als absolute Zahl ausgegeben werden
```{r absolut}
p_rate_abs <- final_df %>%
  group_by(Meldedatum) %>%
  summarise(Hospitalisierung = sum(Hospitalisierung),
            Neuerkrankung = sum(Neuerkrankung))

png(filename="./Bilder/Hospitalisierungsrate_Absolut.png", width= 2000, height = 300)

ggplot(p_rate_abs) +
  geom_line(aes(y = Hospitalisierung, x = Meldedatum), size = 1.25) + 
     xlab("Reported date") + ylab("Hospitalizations") + theme_minimal() +
  theme(text = element_text(size = 25))

dev.off()
```

Es ergeben sich dabei ähnliche Zeitreihen.

Da die Hospitalisierungen immer zum Zeitpunkt der Infektionsmeldung an die 
Gesundheitsämter erfasst wird, i.a.W. also zum Meldedatum,
kann ebenfalls betrachtet werden, wie hoch der Anteil der Hospitalisierten 
Personen an der Gesamtzahl der Infektionen in dieser Woche ist.

```{r hosp durch inf}
p_hosp_inf <- final_df %>%
  group_by(Meldedatum) %>%
  summarise(Hospitalisierung = sum(Hospitalisierung),
            Neuerkrankung = sum(Neuerkrankung)) %>%
  mutate(hosprate = Hospitalisierung / (Neuerkrankung + 1))

png(filename="./Bilder/Hospitalisierungsrate_Anteil.png", width= 2000, height = 900)

ggplot(p_hosp_inf) +
  geom_line(aes(y = hosprate, x = Meldedatum), size = 1.25) +
     xlab("Reported date") + ylab("Hospitalization rate") + theme_minimal() +
  theme(text = element_text(size = 25))

dev.off()
```


Bezüglich dem räumlichen Aspekt bei Meldungen und Hospitalisierungen gilt folgendes:
Die Daten werden in der Regel von dem Gesundheitsamt an das RKI übermittelt, das für den Wohnort der betroffenen Person zuständig ist. In Einzelfällen können die Gesundheitsämter von dieser Regelung abweichen. Wenn die betroffene Person z.B. in Niedersachsen wohnhaft ist, aber in Hamburg hospitalisiert wird, dann erfolgt die Zuordnung zum Bundesland Niedersachsen. Das bedeutet, dass die Daten keinen direkten Rückschluss auf die Krankenhausbelegung der jeweiligen Bundesländer zulassen.
(grafisch PP)
[https://github.com/robert-koch-institut/COVID-19-Hospitalisierungen_in_Deutschland#geografischer-bezug-der-hospitalisierten-covid-19-f%C3%A4lle]


Allerdings werden Infektionen anhand des Infektionsortes gemeldet. 
Somit gilt bspw. für Reisende, wenn diese ihren Wohnort in z.B. Berlin haben, 
aber in Brandenburg positiv getestet werden, die Infektion für das Bundeslnd
Brandenburg gemeldet wird. Sollte sich der gesundheitliche Zustand im zeitlichen 
Verlauf verschlimmern und die Person hospitalisiert werden müssen und sie 
befindet sich noch in Brandenburg, so wird die Hospitalisierung für das
Bundesland Berlin gemeldet.
Somit können Hospitalisierungen nicht ins Verhältnis mit Infektionen gesetzt werden,
da die Hospitalisierung u.U. nicht im Bundesland, in dem die Infektion gemeldet
wurde, gemeldet wird.



Betrachte nun die Hospitalisierung im detail:

```{r}
png(filename="./Bilder/Hospitalisierungsrate_1.png", width= 2000, height = 900)
ggplot(final_df) +
  geom_point(aes(y = Hospitalisierung, x = Meldedatum)) +
  xlab("Reported Date") + ylab("Hospitalizations") + theme_minimal()
dev.off()

png(filename="./Bilder/Hospitalisierungsrate_2.png", width= 2000, height = 900)
ggplot(final_df) +
  geom_point(aes(y = Hospitalisierung/population * 100000, x = Meldedatum)) +
  xlab("Reported Date") + ylab("Hospitalizationrate") + theme_minimal()
dev.off()

png(filename="./Bilder/Hospitalisierungsrate_3.png", width= 2000, height = 900)
ggplot(final_df) +
  geom_point(aes(y = Hospitalisierung, x = Meldedatum, col = Altersgruppe)) +
  xlab("Reported Date") + ylab("Hospitalizationrate") + theme_minimal()
dev.off()

png(filename="./Bilder/Hospitalisierungsrate_4.png", width= 2000, height = 900)
ggplot(final_df) +
  geom_point(aes(y = Hospitalisierung/population * 100000, x = Meldedatum, col = Altersgruppe)) +
  xlab("Reported Date") + ylab("Hospitalizationrate") + 
  facet_wrap(~Bundesland) + theme_minimal()
dev.off()

ggplot(final_df %>% filter(Bundesland == "Bayern")) +
  geom_point(aes(x = Meldedatum, y = Hospitalisierung/population * 100000)) +
  facet_wrap(~Altersgruppe, scales = "free") + 
  ylab("Hospitalizationrate") + xlab("Reported date") + 
  ggtitle("Hospitalizationrate in Bayern") + theme_minimal()
```

Explanation of the Hospitalization using GAM

```{r}
list_of_dfs <- split(final_df, as.factor(final_df$Bundesland))
res <- list()
for (i in names(list_of_dfs)) {
  res[["model"]][[i]] <- gam(Hospitalisierung ~ Altersgruppe +
                   s(index, bs = "ps", k = 100, by = Altersgruppe),
                 data = list_of_dfs[[i]],
                 method = "REML",
                 family = poisson(),
                 knots = list(Kalenderwoche = c(1, 53)))
  res[["summary"]][[i]] <- summary(res[["model"]][[i]])
  #gam.check(bay_gam)
  res[["plot"]][[i]] <- plot(bay_gam)  
}
plot(res$model$Bayern)
summary(res$model$Bayern)

E(Hosp) = exp(b0 + agegroup + f(index ,agegroup))

bay <- gam(Hospitalisierung ~
                   s(index, bs = "ps", k = 100, by = Altersgruppe),
                 data = list_of_dfs$Bayern,
                 method = "REML",
                 family = poisson(),
                 knots = list(Kalenderwoche = c(1, 53)))
plot(bay)
summary(bay)
```

```{r}
p_comp <- final_comp %>% group_by(Meldedatum) %>% filter(Bundesland == "Bayern") %>% 
  filter(Meldedatum <= "2022-02-13") %>%
  summarise(Hospitalisierung = sum(Hospitalisierung)) %>% select(Hospitalisierung) %>%
  ts(frequency = 53, start = c(2020, 1))

p_acf <- final_df %>% group_by(Meldedatum) %>% filter(Bundesland == "Bayern") %>%
  summarise(Hospitalisierung = sum(Hospitalisierung)) 

m_h <- mean(p_acf$Hospitalisierung)
s_h <- sd(p_acf$Hospitalisierung)

p_acf <- p_acf %>%
  #mutate(Hospitalisierung = (Hospitalisierung-m_h)/s_h) %>%
  select(Hospitalisierung) %>%
  ts(frequency = 53, start = c(2020, 1))


p_train <- window(p_acf, end = c(2022,1))
p_test <- window(p_acf, start = c(2022,2))

autoplot(p_acf) + theme_minimal() + ylab("Hospitalization")
# no obvious non-stationarity
acf(p_acf, lag.max = 53)
# strong korrelation up to lag 5
pacf(p_acf)
# using auto.arima for modelidentification
a <- auto.arima(p_acf)
# suggested model: ARIMA(1,0,2)(0,1,0)[53]
png(filename="./Bilder/fc_by.png", width= 2000, height = 900)
plot(forecast(a, h = 2), main = "Forecast Bayern")
lines(p_comp)
dev.off()

trend <- arima(p_acf, order = c(1,0,2))
plot(as.ts(fitted(trend)))
detrend <- p_acf - fitted(trend)
plot(as.ts(detrend))
s <- t(matrix(data = detrend, nrow = 53))
s_p <- colMeans(s, na.rm = T)
plot(as.ts(s_p))
resi <- p_acf - fitted(trend) - s_p
plot(as.ts(resi))

s_p_strich <- diff(s_p, lag = 1)
plot(as.ts(s_p_strich))

png(filename="./Bilder/decomp.png", width= 2000, height = 900)
par(mfrow = c(4,1))
plot(p_acf, ylab = "Hospitalisierung", cex = 2, cex.lab = 2, cex.axis=2, cex.main=2) #+ theme_minimal() + ylab("Hospitalization")
plot(as.ts(fitted(trend)), ylab ="Fitted", main = "ARMA(1,2)", cex.lab = 2,cex = 2,cex.axis=2, cex.main=1.5)
plot(as.ts(rep(s_p, 2)),ylab = "Season", cex = 2,cex.lab = 2,cex.axis=2, cex.main=2)
plot(as.ts(resi),ylab = "Residuals", cex = 2,cex.lab = 2,cex.axis=2, cex.main=2)
dev.off()

png(filename="./Bilder/fc_bay.png", width= 2000, height = 900)
plot(forecast(a, h = 2), main = "Forecast Bayern using ARIMA(1,0,2)(0,1,0)[53]",
     ylab = "Hospitalization", xlab = "Time")
dev.off()

## accuracy
b <- arima(p_train, order = c(1,0,2), seasonal = list(order = c(0,1,0), period = 53))
accuracy(forecast(b, h = 1), p_test)
```


```{r}
p_comp <- final_comp %>% group_by(Meldedatum) %>% filter(Bundesland == "Niedersachsen") %>% 
  filter(Meldedatum <= "2022-02-13") %>%
  summarise(Hospitalisierung = sum(Hospitalisierung)) %>% select(Hospitalisierung) %>%
  ts(frequency = 53, start = c(2020, 1))

p_acf <- final_df %>% group_by(Meldedatum) %>% filter(Bundesland == "Niedersachsen") %>%
  summarise(Hospitalisierung = sum(Hospitalisierung)) 

m_h <- mean(p_acf$Hospitalisierung)
s_h <- sd(p_acf$Hospitalisierung)

p_acf <- p_acf %>%
  #mutate(Hospitalisierung = (Hospitalisierung-m_h)/s_h) %>%
  select(Hospitalisierung) %>%
  ts(frequency = 53, start = c(2020, 1))


p_train <- window(p_acf, end = c(2022,1))
p_test <- window(p_acf, start = c(2022,2))

autoplot(p_acf) + theme_minimal() + ylab("Hospitalization")
# no obvious non-stationarity
acf(p_acf, lag.max = 53)
# strong korrelation up to lag 5
pacf(p_acf)
# using auto.arima for modelidentification
a <- auto.arima(p_acf)
# suggested model: ARIMA(0,1,0)(0,1,0)[53]
png(filename="./Bilder/fc_ns.png", width= 2000, height = 900)
plot(forecast(a, h = 2), main = "Forecast Niedersachsen using ARIMA(0,1,0)(0,1,0)[53]")
lines(p_comp)
dev.off()

b <- arima(p_train, order = c(0,1,0), seasonal = list(order = c(0,1,0), period = 53))
accuracy(forecast(b, h = 2), p_test)
```


Bundesländer in Deutschland unterscheiden sich sehr bezüglich ihrer 
sozioökonomischen Faktoren, wie z.B. der Altersstruktur, dem Reichtum,
der Bildung oder durch den Anteil der Städtischen und Ländlichen Gebiete.
Insgesamt können Unterschiede zwischen Bundesländern aufgrund dieser Faktoren
erklärt werden.

Für die Hospitalisierung sind Faktoren wie das Alter, Vorerkrankungen oder der
Impfschutz entscheidene Faktoren, um nur ein paar zu nennen.
Somit kann die Hospitalisierungsrate erklärt werden durch
Hospitalisierungsrate|Bundesland, wobei der Effekt des Bundeslandes auf die 
Hospitalisierunsrate durch die Altersstuktur des Bundeslandes und die Impfquote
erklärt werden kann.

Des Weiteren hängt die Hospitalisierungsrate von der vorherrschenden 
Variante des Corona-Virus ab. So zeigt sich in aktuellen Studien, 
dass Omikron zwar die höchste Ansteckungsrate aufweist, allerdings auch in den
wenigsten Fällen zu schweren Verläufen führt.

Zu letzt hängt die Hospitalisierungsrate von der Höhe der Infektionen ab.
Dabei kann der Effekt der Infektionen ebenfalls anhand des Effektes des Bundeslandes
geschätzt werden, ist allerdings unabhängig vom Impfstatus.

Insgesamt ergibt sich somit
E(Hospitalisieurngsrate_i) = Bundesland_i + Infektionen_i +  Variante_i, wobei gilt

Hospitalisierung_i|Bundesland_i kann vollständig durch die Population erklärt werden.
Es ergibt sich somit
E(Hospitalisieurngsrate_i) = Altersgruppe_i + Impfquote_i + Infektionen_i +  Variante_i

Durch Aufnahme einer Zeitvariierenden Kovariable kann der Effekt der Impfquote und
der Variante geschätzt werden, da hierfür die Datenlage nicht ausreichend gegeben ist.

Es ergibt sich als neue Gleichung

E(Hospitalisieurngsrate_i) = f(zeit_i) + Altersgruppe_i + Infektionen_i

Im Folgenden werden nun die eben theoretisch hergeleiteten Erkenntnisse überprüft.

Unterschied zwischen den Bundesländern
```{r hosprate bundesland}
p_bl_age <- final_df %>%
  group_by(Bundesland, Meldedatum, Kalenderjahr, Kalenderwoche) %>%
  #filter(Bundesland %in% c("Bayern", "Sachsen", "Thueringen", "Saarland")) %>%
  summarise(Hospitalisierung = sum(Hospitalisierung),
            population = sum(population)) %>%
  mutate(hosprate = Hospitalisierung/population * 100000)

png(filename="./Bilder/Hosprate_Bundesland2.png", width= 2000, height = 900)

ggplot(p_bl_age) +
  geom_line(aes(x = Meldedatum, y = hosprate, col = Bundesland), size = 1.25) + theme_minimal() +
  theme(text = element_text(size = 25))  +
  xlab("Reported date") +
  ylab("Hospitalization rate") 
  #scale_color_brewer(palette = "Set1") #+
  # gghighlight(label_params = list(size = 8))

dev.off()
```
Wie zu sehen ist, gibt es deutliche Unterschiede bezüglich der Hospitalisierungs-
rate in den einzelnen Bundesländern.

Es wird nun überprüft, inwiefern sich die Altersgruppen bzgl der Hospitalisierungsrate
unterscheiden.

```{r}
p_age <- final_df %>%
  group_by(Meldedatum, Altersgruppe) %>%
  summarise(Hospitalisierung = sum(Hospitalisierung),
            population = sum(population)) %>%
  mutate(hosprate = Hospitalisierung / population * 100000)

# Level reverse für die Legende der Graphik
p_age$Altersgruppe <- fct_rev(p_age$Altersgruppe)

# AlterColors <- c("#8e0152","#c51b7d","#de77ae","#7fbc41","#4d9221","#276419")
# AlterColors <- c("#9e0142","#d53e4f","#f46d43","#66c2a5","#3288bd","#5e4fa2")
AlterColors <- c("#762a83","#9970ab","#c2a5cf","#a6dba0","#5aae61","#1b7837")

png(filename="./Bilder/Hosprate_Altersgruppen.png", width= 2000, height = 900)

ggplot(p_age) +
  geom_line(aes(x = Meldedatum, y = hosprate, col = Altersgruppe), size = 1.25) +
  xlab("Reported date") + ylab("Hospitalization rate") + theme_minimal() +
  theme(text = element_text(size = 25))  +
  scale_color_manual(values = AlterColors)
  

dev.off()
```
Bezüglich der Altersgruppen ergeben sich ebenfalls extrem unterschiedliche 
Hospitalisierungsraten. 

Betrachtet man nun die Hospitalisierungsrate gegeben der Bundesländer und der
Altersgruppen, sowie deren Population, sollten sich bereits ähnlichere Verläufe zeigen

```{r}
p_bl_age <- final_df %>%
  mutate(hosprate = Hospitalisierung / population * 100000)

p1 <- ggplot(p_bl_age %>% filter(Altersgruppe == "00-04") %>%
  filter(Bundesland %in% c("Bayern", "Sachsen", "Thueringen", "Saarland"))) +
  geom_line(aes(x = Meldedatum, y = hosprate, col = Bundesland), size = 1) + theme_minimal() +
  scale_color_brewer(palette = "Set1") +
  labs(title = "00-04", y = "Hospitalisierungsrate") +
  theme(text = element_text(size = 20))
p2 <- ggplot(p_bl_age %>% filter(Altersgruppe == "05-14") %>%
  filter(Bundesland %in% c("Bayern", "Sachsen", "Thueringen", "Saarland"))) +
  geom_line(aes(x = Meldedatum, y = hosprate, col = Bundesland), size = 1) + theme_minimal() +
  scale_color_brewer(palette = "Set1") +
  labs(title = "05-14", y = "Hospitalisierungsrate") +
  theme(text = element_text(size = 20))
p3 <- ggplot(p_bl_age %>% filter(Altersgruppe == "15-34") %>%
  filter(Bundesland %in% c("Bayern", "Sachsen", "Thueringen", "Saarland"))) +
  geom_line(aes(x = Meldedatum, y = hosprate, col = Bundesland), size = 1) + theme_minimal() +
  scale_color_brewer(palette = "Set1") +
  labs(title = "15-34", y = "Hospitalisierungsrate") +
  theme(text = element_text(size = 20))
p4 <- ggplot(p_bl_age %>% filter(Altersgruppe == "35-59") %>%
  filter(Bundesland %in% c("Bayern", "Sachsen", "Thueringen", "Saarland"))) +
  geom_line(aes(x = Meldedatum, y = hosprate, col = Bundesland), size = 1) + theme_minimal() +
  scale_color_brewer(palette = "Set1") +
  labs(title = "35-59", y = "Hospitalisierungsrate") +
  theme(text = element_text(size = 20))
p5 <- ggplot(p_bl_age %>% filter(Altersgruppe == "60-79") %>%
  filter(Bundesland %in% c("Bayern", "Sachsen", "Thueringen", "Saarland"))) +
  geom_line(aes(x = Meldedatum, y = hosprate, col = Bundesland), size = 1) + theme_minimal() +
  scale_color_brewer(palette = "Set1") +
  labs(title = "60-79", y = "Hospitalisierungsrate") +
  theme(text = element_text(size = 20))
p6 <- ggplot(p_bl_age %>% filter(Altersgruppe == "80+") %>%
  filter(Bundesland %in% c("Bayern", "Sachsen", "Thueringen", "Saarland"))) +
  geom_line(aes(x = Meldedatum, y = hosprate, col = Bundesland), size = 1) + theme_minimal() +
  scale_color_brewer(palette = "Set1") +
  labs(title = "80+", y = "Hospitalisierungsrate") +
  theme(text = element_text(size = 20))

png(filename="./Bilder/Hosprate_geg_bl_aget.png", width= 2000, height = 900)

grid.arrange(p1,p2,p3,p4,p5,p6)

dev.off()
```

Es ergeben sich also sehr ähnliche Verläufe, wobei für niedrigere Altersgruppe
die Unterschiede etwas stärker sind.

Betrachtet man nun noch die Impfquote, so würden noch ähnlichere Verläufe erwartet
werden.
Da keine Daten bezüglich des Impfstatus für die hospitalisierten Fälle vorliegen,
und sich die Daten des RKI bezüglich der Hospitalisierung und der Impfung stark
unterscheiden. Wird hier zitiert von folgender Quelle.....

---Hier wird dringend eine Grafik benötigt.---


Das heißt, durch das aufteilen des Bundeslandeffektes kann dieser vollständig
rausgerechnet werden.
Es ergibt sich als Modell
Hospitalisierungsrate_i = beta0 + sum(beta_Altersgruppe_j * 1(x_i = j)) + 
  s(time_i)

für j aus {0-4,...,80+}

--------------------------------------------------------------------------------

Schauen wir uns als nächstes die Neuerkrankungen und Hospitalisierungen an

```{r scatter}
p_scatter <- final_df %>%
  group_by(Bundesland, Altersgruppe, Meldedatum, Kalenderwoche, Kalenderjahr) %>%
  summarise(Hospitalisierung = sum(Hospitalisierung),
            Neuerkrankung = sum(Neuerkrankung))

png(filename="./Bilder/Scatterplot_Hosp_Infektion.png", width= 2000, height = 900)

ggplot(p_scatter) +
  #geom_smooth(aes(x = Neuerkrankung, y = Hospitalisierung, col = Altersgruppe), method = "lm") +
  geom_point(aes(x = Neuerkrankung, y = Hospitalisierung)) + theme_minimal() +
  xlab("New cases") + ylab("Hospitalizations") +
  theme(text = element_text(size = 25)) +
  ylim(c(0, 1200)) +
  xlim(c(0, 87500))

dev.off()
```

```{r}
p_neuer <- final_df %>%
  group_by(Bundesland, Altersgruppe, Meldedatum, Kalenderwoche, Kalenderjahr) %>%
  summarise(Hospitalisierung = sum(Hospitalisierung),
            Neuerkrankung = sum(Neuerkrankung))

# Factor relevel für Legende
p_neuer$Altersgruppe <- fct_rev(p_neuer$Altersgruppe)

png(filename="./Bilder/Scatterplot_Altersgruppen2.png", width= 2000, height = 900)

ggplot(p_neuer) +
  geom_smooth(aes(x = Neuerkrankung, y = Hospitalisierung, col = Altersgruppe), method = "lm", size = 2) +
  geom_point(aes(x = Neuerkrankung, y = Hospitalisierung, col = Altersgruppe)) +
  xlab("New cases") + ylab("Hospitalizations") +
  theme_minimal() + #+ facet_wrap(~Altersgruppe)
  theme(text = element_text(size = 25), legend.position = "none")  +
  scale_color_manual(values = AlterColors) +
  ylim(c(0, 1200)) +
  xlim(c(0, 87500))

dev.off()

```
Der Zusammenhang ist also nicht linear.
Desweiteren sind hier Interaktionseffekte zwischen der Neuerkrankung und den
Altersgruppen ersichtlich.
Die Neuerkrankung sollte also nicht linear mit ins Modell aufgenommen werden.
Außer für die Altersgruppe der 80+ jährigen ist die Annahme der nicht-linearität
erfüllt.

Nun mit Bundesland als weitere Kovariable

```{r}
p_neuer_bl <- final_df %>%
  group_by(Altersgruppe, Meldedatum, Kalenderwoche, Kalenderjahr, Bundesland) %>%
  summarise(Hospitalisierung = sum(Hospitalisierung),
            Neuerkrankung = sum(Neuerkrankung))

# Factor relevel für Legende
p_neuer_bl$Altersgruppe <- fct_rev(p_neuer_bl$Altersgruppe)

png(filename="./Bilder/Scatterplot_Altersgruppen_Bundesland.png", width= 2000, height = 900)

ggplot(p_neuer_bl) +
  geom_smooth(aes(x = Neuerkrankung, y = Hospitalisierung, col = Altersgruppe), method = "lm") +
  geom_point(aes(x = Neuerkrankung, y = Hospitalisierung, col = Altersgruppe))  +
  scale_color_manual(values = AlterColors) +
  facet_wrap(~Bundesland) +
  xlab("New cases") + ylab("Hospitalizations") +
  theme_minimal() +
  theme(text = element_text(size = 20))

dev.off()
```

Extreme Unterschiede. Unter Umständen aufgrund von höheren Tourismusaktivitäten,
aber auch durch eine höhere Poplation vermutet, unterschiede im Anteile 
Urbanisierter zu länderlichen Gebieten, Maßnahmen in der Coronapolitik usw.

--------------------------------------------------------------------------------




--------------------------------------------------------------------------------

Im Gegensatz zur Hospitalisierung sind die Neuerkrankungen jedoch abhängig
voneinander.
Das heißt, die Hospitalisierungszahl zum Tag i ist unabhängig zur Hospitalisierungszahl
zum Tag i - 1.
Allerdings gilt, dass die Infektionen abhängig sind, wodurch die Annahme von unabh
Beobachtungen nicht gegeben ist.
Somit ist die Hospitalisierungszahl|Infektionen zum Tag i ebenfalls abhängig
von der Hospitalisierungszahl|Infektionen zum Tag i + 1

Es wird also nun untersucht, inwiefern sich die Korrelation auswirkt

```{r korr}
p_korr <- final_df %>%
  group_by(Altersgruppe, Meldedatum, Kalenderwoche, Kalenderjahr) %>%
  summarise(Hospitalisierung = sum(Hospitalisierung),
            Neuerkrankung = sum(Neuerkrankung))

acf(p_korr$Neuerkrankung)
pacf(p_korr$Neuerkrankung)

```

Die Autokorrelationsfunktion weist auf stark korrelierte Beobachtungen bis 
einschließlich lag 10 hin,
dahingegen sehen die partiellen Autokorrelationen recht gut aus. Es ist 
ein gewisser Saisonaler Trend ersichtlich






Variablen in dem Datensatz:
Das Meldedatum ergibt sich aus dem Datum bei Hospitalisierung und Meldedatum 
bei Infektion.
Das Datum des Hospitalisierungsdatensatzes entspricht dem Berichtsdatum der 
7-Tage Hospitalisierungsinzidenz.
Das Berichtsdatum ist das Datum, an welchem die 7-Tages-Inzidenz tagesaktuell
vom RKI berichtet wird. Dabei gilt das gleiche Verhalten wie für das Meldedatum.
Zum Berichtsdatum werden also alle Fälle vom RKI kommuniziert, die in den 
letzten 7 Tagen vorgefallen sind.
Somit können wir schlussfolgenern, dass alle hospitalisierten Fälle der letzten 
7 Tage, zum Berichtsdatum, ebenfalls als positiv getestet an die Gesundheitsämter
übermittelt wurden.
Sollte die Hospitalisierung später als 7 Tage stattfinden, so wird der Zahl 
entsprechend nachgemeldet.
Da es sich um einen aktualisierten Datensatz handelt können wir also jede Infektion
und jede Hospitalisierung eindeutig einer Woche zuordnen, wodurch sich 
die Hospitalisierungsrate als Hosp_i/Neuerk_i i = 1,...,n ergibt,
wobei i die entsprechende Woche seit Beginn der Corona-Pandemie darstellt.
In anderen Worten, sämtliche Hospitalisierungen können eindeutig den Infektionen
innerhalb einer Woche zugeordnet werden, und somit ergeben sich die Hospitalisierungen
nicht aus den Infektionen aus unterschiedlichen Wochen, sondern aus genau einer.

Als nächstes wollen wir uns einzelne Kohorten anschauen und wie diese die 
Hospitalisierung beeinflussen. 
Es ist allgemein bekannt, dass Kinder (5-14) und junge Erwachsene weniger stark
zur Hospitalisierungszahl beitragen, wohingegen die ältere Bevölkerung eher hospitalisiert
wird, aufgrund eines schweren Krankheitsverlaufs. 
Auch die Ansteckung ist verschoben, da sich Kinder und junge Erwachsene 
eher in gesellschaftlichen Interaktionen (Schule, Arbeit, etc.) anstecken und
sich hier Ausbrüche entwickeln. Diese allerdings erst im Verlauf die ältere 
Bevölkerung anstecken.
So stand 2020 die Inzidenz bei Kindern und das Vorkommen von Ausbrüchen in 
Schulen in einem engen Zusammenhang mit der Inzidenz in der Allgemeinbevölkerung
[https://link.springer.com/article/10.1007/s00103-021-03391-0]
Mit der Verbreitung der leichter übertragbaren besorgniserregenden Virusvariante 
Alpha (B.1.1.7) ab Anfang 2021 erhöhten sich die Inzidenz bei Kindern im 
Kindergarten- und Schulalter und die Anzahl der Ausbrüche in Schulen und in der 
Kinderbetreuung.
[https://link.springer.com/article/10.1007/s00103-021-03391-0]


```{r }
p1 <- final_df

levels(p1$Altersgruppe) <- c("00-04", "05-59", "05-59", "05-59", "60+", "60+")
p1 <- p1 %>%
  group_by(Bundesland, Altersgruppe, Meldedatum, Kalenderjahr, Kalenderwoche) %>%
  summarise(Neuerkrankung = sum(Neuerkrankung),
            Hospitalisierung = sum(Hospitalisierung)) %>%
  summarise(hosprate = (Hospitalisierung) / (Neuerkrankung + 1))

ggplot(p1) +
  geom_line(aes(x = Meldedatum, y = hosprate, col = Altersgruppe)) +
  facet_wrap(~Bundesland)

p2 <- final_df
levels(p2$Altersgruppe) <- c("00-04", "05-59", "05-59", "05-59", "60+", "60+")
p2 <- p2 %>%
  group_by(Bundesland, Altersgruppe, Meldedatum, Kalenderjahr, Kalenderwoche) %>%
  summarise(Neuerkrankung = sum(Neuerkrankung),
            Hospitalisierung = sum(Hospitalisierung))

ggplot(p2) +
  geom_line(aes(x = Meldedatum, y = Neuerkrankung, col = Altersgruppe)) +
  facet_wrap(~Bundesland)

acf(final_df$Hospitalisierung[final_df$Bundesland == "Bayern"])
pacf(final_df$Hospitalisierung[final_df$Bundesland == "Bayern"])
```

```{r test}

p_test <- arima(p_ts, order = c(0, 1, 1))
p_test
p_test_fitted <- p_ts - residuals(p_test) 
ts.plot(p_ts)
points(p_test_fitted, type = "l", col = 2, lty = 2)


p_bydate <- final_df %>%
  group_by(Meldedatum, Altersgruppe) %>%
  summarise(Hospitalisierung = sum(Hospitalisierung),
            Neuerkrankung = sum(Neuerkrankung))

p_ts <- as.ts(data.frame(Hospitalisierung = p_bydate$Hospitalisierung, Altersgruppe = p_bydate$Altersgruppe))
p_ts
plot.ts(p_ts, plot.type = "multiple", main = " Hospitaliserungen",
        xlab = "Wochen seit Beginn")

p_test <- arima(p_ts, order = c(0, 1, 1))
p_test
p_test_fitted <- p_ts - residuals(p_test) 
ts.plot(p_ts)
points(p_test_fitted, type = "l", col = 2, lty = 2)

```

Da sich die Personen in einer Altersgruppe in Hinsicht auf die für die Infizierung
relevanten Eigenschaften ähnlicher sein dürften als beliebig ausgewählte Personen, 
sind die Infektionen in einer Altersgruppe vermutlich positiv korreliert, 
so dass die Annahme unabhängiger Beobachtungen des klassischen linearen Modells 
nicht erfüllt sind.
So gibt es bei Personen der Altersgruppe 05-14 häufiger Covid-Ausbrüche in 
Schulen, wodurch eine Infektion in dieser Altersgruppe, zumindest in bestimmten
Gebieten abhängig ist, von Infektionen anderer Personen in der gleichen 
Altersgruppe.
Wir gehen also davon aus, dass ebenfalls die Hospitalisierungen davon betroffen
sind, da eine höhere Infektionszahl i.A. gleichbedeutend mit einer höheren Anzahl
an Hospitalisierten einhergeht (Vergleich siehe Grafik).

Hier sieht man auch direkt die hohe Korrelation zwischen Infektion
und Hospitalisierung über den gesamten zeitlichen Verlauf, sowie 
die unterschiedlichen Effekte der Hospitalisierung auf unterschiedlichen 
Altersgruppen (Interaktionseffekte)
```{r}
ggplot(final_df %>% filter(Altersgruppe == "00-04")) +
  geom_line(aes(x = Meldedatum, y = Hospitalisierung, col = "a")) +
  geom_line(aes(x = Meldedatum, y = Neuerkrankung, col = "b")) +
  facet_wrap(~Bundesland) +
  scale_y_log10(name = "Hospitalisierung/Infizierung auf log10 Skala") +
  scale_color_manual(values = c("a" = "red", "b" = "blue"), 
                     labels = c("Hospitalisiert", "Infiziert"),
                     name = "Legende") +
  theme_minimal()

ggplot(final_df %>% filter(Altersgruppe == "80+")) +
  geom_line(aes(x = Meldedatum, y = Hospitalisierung, col = "a")) +
  geom_line(aes(x = Meldedatum, y = Neuerkrankung, col = "b")) +
  facet_wrap(~Bundesland) +
  scale_y_log10(name = "Hospitalisierung/Infizierung auf log10 Skala") +
  scale_color_manual(values = c("a" = "red", "b" = "blue"), 
                     labels = c("Hospitalisiert", "Infiziert"),
                     name = "Legende") +
  theme_minimal()
```

Die Hospitalisierungzahl weist einen saisonalen Effekt auf, der bedingt durch
die Infektionen entsteht.
Sind die Infektionen somit hoch, so ist die Hospitalisierungszahl hoch

```{r}
p_sai <- final_df %>%
  group_by(Meldedatum, Kalenderjahr, Kalenderwoche) %>%
  summarise(Hospitalisierung = sum(Hospitalisierung))

png(filename="./Bilder/Zyklus_Muster.png", width= 2000, height = 900)

ggplot() +
  geom_line(data = p_sai %>% filter(Kalenderjahr == "2020"), 
            aes(x = Kalenderwoche, y = Hospitalisierung, col = "a"), size = 1.25) +
  geom_line(data = p_sai %>% filter(Kalenderjahr == "2021"), 
            aes(x = Kalenderwoche, y = Hospitalisierung, col = "b"), size = 1.25) +
  geom_line(data = p_sai %>% filter(Kalenderjahr == "2022"), 
            aes(x = Kalenderwoche, y = Hospitalisierung, col = "c"), size = 1.25) +
  scale_color_manual(name = "Legende", values = c("a" = "#1b9e77", "b" = "#d95f02", "c" = "#e7298a"),
                     labels = c("2020", "2021", "2022")) + theme_minimal() +
  theme(text = element_text(size = 25)) +
  xlab("Calendar week") + ylab("Hospitalizations")

dev.off()
```

Die Hospitalisierungsrate hingegen ist nicht saisonal, weist aber einen negativen
Trend auf, ist also somit nicht stationär.

```{r}
p_sai_rate <- final_df %>%
  group_by(Meldedatum, Kalenderjahr, Kalenderwoche) %>%
  summarise(Hospitalisierung = sum(Hospitalisierung), 
            Neuerkrankung = sum(Neuerkrankung)) %>%
  mutate(hosprate = Hospitalisierung/(Neuerkrankung + 1))

ggplot(p_sai_rate) +
  geom_line(aes(x = Meldedatum, y = hosprate)) + theme_minimal()

```


Zusammefassung:
Die Hospitalisierung ist für unterschiedliche Altersgruppen unterschiedlich. 
Die Altersgruppen sind somit ein Merkmal, welches Berücksichtigt werden sollte.
Des Weiteren ist die Hospitalisierungsrate in den einzelnen Bundesländern 
Deutschlands, bedingt auf die Altersgruppen, unterschiedlich. Daher sollte
ebenfalls eine räumliche Trennung vorgenommen werden.
Es ergeben sich die beiden Darstellung der Hospitalisierung als  
- Hospitalisierungsrate  
- Hospitalisierung in absoluten Zahlen  
Wie sich zeigt, ist die Hospitalisierungsrate nicht saisonal und 
nicht stationär.
Hingegen sind die Hospitalisierungen in absoluten Zahlen saisonal und ebenfalls
nicht stationär.

```{r}
p_lag <- as_tsibble(p_sai_rate)

p_lag %>%
  gg_lag(Hospitalisierung, geom = "point")
```
Stark positiver Zusammenhang bei lag 1, lag 2 und lag 3

```{r}
p_sai_rate$index <- 1:nrow(p_sai_rate)
p_arima <- as_tsibble(p_sai_rate, index = index)
p_arima <- p_arima[,c(3,4)]
forecast::auto.arima(p_arima, stationary = F, seasonal = T)
hist(p_arima$Hospitalisierung)
p_gamm <- gamm(Hospitalisierung ~ s(index, k = 100, bs = "ps"),
               family = "poisson", data = p_arima[-c(nrow(p_arima) - 1, nrow(p_arima)), ],
               correlation = corARMA(form = ~1|index, p = 1, q = 1))
summary(p_gamm$gam)
gam.check(p_gamm$gam)
plot(p_arima$Hospitalisierung~p_arima$Meldedatum, type = "l", col = "red")
lines(predict(p_gamm$gam, type = "response", newdata = p_arima)~p_arima$Meldedatum, type = "l", col = "black")
lines(predict(p_gamm$gam, type = "response", newdata = p_arima[c(nrow(p_arima) - 1, nrow(p_arima)), ])~p_arima$Meldedatum[c(nrow(p_arima) - 1, nrow(p_arima))], type = "l", col = "black")
```
