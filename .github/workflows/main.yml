# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      - name: Send Email to Phu
        uses: devellany/send-mail@v1.0.2
        with:
         host: smtp.google.com
         account: ${{secrets.ACCOUNT}}
         password: ${{secrets.MAIL_PASSWORD}}
         sender: Alex
         from: ${{secrets.MAIL_ADDRESS}}
         to: Nguyen.Phu@campus.lmu.de
         subject: Covid19-Hospitalisierung Aenderungen auf dem main
         body: Hey Phu, es gab eine Aenderung auf dem main. Schau doch mal rein https://github.com/amarquard089/Covid19-Hospitalisierungsrate
         contentType: text/html
         
      - name: Send Email to Qian
        uses: devellany/send-mail@v1.0.2
        with:
         host: smtp.google.com
         account: ${{secrets.ACCOUNT}}
         password: ${{secrets.MAIL_PASSWORD}}
         sender: Alex
         from: ${{secrets.MAIL_ADDRESS}}
         to: Feng.Qian@campus.lmu.de
         subject: Covid19-Hospitalisierung Aenderungen auf dem main
         body: Hey Phu, es gab eine Aenderung auf dem main. Schau doch mal rein https://github.com/amarquard089/Covid19-Hospitalisierungsrate
         contentType: text/html
         
      - name: Send Email to Johannes
        uses: devellany/send-mail@v1.0.2
        with:
         host: smtp.google.com
         account: ${{secrets.ACCOUNT}}
         password: ${{secrets.MAIL_PASSWORD}}
         sender: Alex
         from: ${{secrets.MAIL_ADDRESS}}
         to: J.Pfeifer@campus.lmu.de
         subject: Covid19-Hospitalisierung Aenderungen auf dem main
         body: Hey Johannes, es gab eine Aenderung auf dem main. Schau doch mal rein https://github.com/amarquard089/Covid19-Hospitalisierungsrate
         contentType: text/html
         
      - name: Send Email to Alex
        uses: devellany/send-mail@v1.0.2
        with:
         host: smtp.google.com
         account: ${{secrets.ACCOUNT}}
         password: ${{secrets.MAIL_PASSWORD}}
         sender: Alex
         from: ${{secrets.MAIL_ADDRESS}}
         to: A.Marquard@campus.lmu.de
         subject: Covid19-Hospitalisierung Aenderungen auf dem main
         body: Hey Johannes, es gab eine Aenderung auf dem main. Schau doch mal rein https://github.com/amarquard089/Covid19-Hospitalisierungsrate
         contentType: text/html
